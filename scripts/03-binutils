name=binutils
version=2.32

msg "$name -> $CROSS_PREFIX"

build(){
	cd $name-$version

	mkdir build && cd build

	../configure --target=${TARGET_TRIP} \
		--prefix=${CROSS_PREFIX} \
		--with-sysroot \
		--disable-nls \
		--disable-werror \
		--disable-multilib

	make -j$(nproc)
	make install
}
